{
  "name": "@autobs/server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "type-check": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test test/**/*.test.ts"
  },
  "dependencies": {
    "@autobs/browser-pool": "workspace:*",
    "@autobs/logger": "workspace:*",
    "@autobs/pipeline": "workspace:*",
    "@autobs/storage": "workspace:*",
    "@autobs/task-orchestrator": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.2",
    "vitest": "^2.1.8"
  }
}
